/**
 * 슬롯 착장 도식 좌표 (Phase 2 UI)
 *
 * 값은 도식 컨테이너 기준 백분율 [x%, y%]이며, 프론트 SlotDiagram이 띄우는
 * `public/slots/figure-front.png`(정면 전신, 3:4) 위 실제 위치를 측정해 정한 값이다.
 * 도식 이미지를 교체하면 이 좌표도 다시 측정해야 한다.
 *
 * ⚠️ 좌/우 기준 — 슬롯 이름의 좌·우는 **착용한 선수 기준**이다(의류 표기 관례).
 *   도식은 선수를 정면에서 본 그림이므로 선수의 왼쪽은 화면에서는 오른쪽에 온다.
 *   따라서 `_L` 슬롯은 x > 50, `_R` 슬롯은 x < 50 에 놓인다. (2026-07-29 좌우 반전 수정)
 *
 * 이미지 기준선 (y%):
 *   모자 크라운 4~8.3 / 모자챙 8.5~10.5 / 얼굴 11~17 / 카라 17~21 / 어깨 19~22
 *   소매 22~31 / 상의 21~41 / 벨트 40~42 / 하의 42~92 / 신발 92~96
 */
export const SLOT_DISPLAY_COORDS: Record<string, [number, number]> = {
  // 모자 — 크라운 y4~8.3, 챙 y8.5~10.5(그 아래는 얼굴). 좁은 영역에 5개가 몰려 균등 배치한다.
  CAP_BACK: [50, 4.5],
  CAP_FRONT: [50, 7.0],
  CAP_SIDE_L: [54.5, 6.2], // 선수의 왼쪽 = 화면 오른쪽
  CAP_SIDE_R: [45.5, 6.2],
  CAP_BRIM_TOP: [50, 9.5],

  // 상의 — 카라 안쪽 → 어깨 상단 → 어깨 끝(소매 이음선) 순으로 벌어진다
  COLLAR_L: [53.5, 19],
  COLLAR_R: [46.5, 19],
  BACK_SHOULDER_L: [58.5, 20.5],
  BACK_SHOULDER_R: [41.5, 20.5],
  SHOULDER_LINE_L: [63, 22],
  SHOULDER_LINE_R: [37, 22],
  SLEEVE_L: [63.5, 27],
  SLEEVE_R: [36.5, 27],
  CHEST_L: [56.5, 26],
  CHEST_R: [43.5, 26],

  // 하의 — 좌우가 아니라 '타격 방향' 기준이라 반전 대상이 아니다.
  // 해당 높이의 바지 바깥 끝선이 x 61 부근이다.
  PANTS_HIP_SIDE_FACING: [59.5, 47],
  PANTS_THIGH_SIDE_FACING: [59.5, 58],
};
